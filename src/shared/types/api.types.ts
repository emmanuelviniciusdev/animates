export type ResponseError = {
    error: boolean
    message: string
}

export type ResponseSuccess = {
    data: any
}

export type DomainItem = {
    id: number
    nm_dominio: string
    tp_dominio: string
}
